function Loading() {
    return (
        <main className="text-center">
            <h1>Loading...</h1>
            <p>Please wait while the page is loading</p>
        </main>
    );
}

export default Loading;